name: Fix image fallback test env shell once (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: [self-hosted, Linux, X64, ci, isolated]
    steps:
      - run: echo "Retired workflow; no action performed."
